steps:
  - task: DownloadSecureFile@1
    name: privateSigningKey
    displayName: download private signing key
    inputs:
      secureFile: "be.stateless.snk"
  - powershell: |
      Write-Host "##vso[task.setvariable variable=PrivateSigningKeyPath;]$(privateSigningKey.secureFilePath)"
    displayName: udpate private signing key path