steps:
  - task: PowerShell@2
    inputs:
      filePath: '$(Build.SourcesDirectory)\t\Scripts\pack-powershell-module.ps1'
      arguments: '-Path $(Build.SourcesDirectory)\m -Destination $(Build.ArtifactStagingDirectory) -ModuleVersion $(Build.BuildNumber) -CertificateFilePath $(CodeSigningCertificatePath) -CertificatePassword $(ConvertTo-SecureString "$(certificate-password)" -AsPlainText -Force)'
    displayName: pack PowerShell module
